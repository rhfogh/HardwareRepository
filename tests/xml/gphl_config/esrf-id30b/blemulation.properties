# These need to be the same as gonio_axis_names and gonio_centring_axis_names
# in instrumentation.in

co.gphl.beamline.rotation_axis_names = omega kappa phi
co.gphl.beamline.centring_axis_names = phiy sampy sampx
co.gphl.beamline.centring_axis_limits = -0.5 0.5 -0.5 0.5 -0.5 0.5
#
# Beamstop parameters are only needed here if they are not present
# in instrumentation.nml
#co.gphl.beamline.beamstop.id = 7135b6a3-fbc7-4c87-b64a-b644e6a7be44
#co.gphl.beamline.beamstop.paramNames = BEAM_STOP_RADIUS    BEAM_STOP_S_LENGTH   BEAM_STOP_S_DISTANCE
#co.gphl.beamline.beamstop.paramValues = 2.0                 -120.0                60.0

# These two are written to the corresponding variables SIMCAL_OPTIONS_LIST%N_RAYS
# and ERROR_MODEL_LIST%BACKGROUND in the simcal input file. If not specified,
# the defaults are 1000 and 10.0 respectively (defined as static constants
# in the SimcalLauncher class).
co.gphl.beamline.simcal.nrays = 4000
co.gphl.beamline.simcal.background = 2.0
